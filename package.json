{
  "name": "search-engine-vercel",
  "version": "1.0.0",
  "description": "Search Engine MVP deployed on Vercel",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm start"
  },
  "dependencies": {
    "meilisearch": "^0.31.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 